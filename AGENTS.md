# PressWatch リポジトリ指示

応答と作成する文書は原則日本語とし、既存文書の言語は維持してよい。

## 作業範囲と承認

- 変更前に対象と除外事項を示し、承認済みの範囲で調査・編集・検証を進める。
- ユーザーの明示的な方針変更や承認は、その対象範囲で本ファイル、`task.md`、スキル、docsより優先する。
  意図や適用範囲が不明で矛盾を解消できない場合は、該当する編集だけ止めて確認する。
- 同じ承認を重ねて求めず、`task.md`等で明示された段階別の承認は守る。
- `.env`、`.env.*`、認証情報・秘密鍵ファイルは明示的な許可なく読まず、秘密情報を出力や変更内容に含めない。
- ユーザーの既存変更を破棄・上書きしない。

## 情報の正本

今回の作業に関係する箇所だけ確認する。

- `task.md`: 一タスクの目的、範囲、除外事項、確認条件（Git管理外）。
- `docs/requirements.md`: 製品要件とMVP範囲。
- `docs/tasks.md`: 全体の進捗と完了状態。
- `docs/tech-stack.md`と各マニフェスト: 技術選定とバージョン。
- `docs/local-development.md`と各スクリプト定義: 実行・検証コマンド。

継続して共有する要件・設計・手順は`docs/`に置き、`task.md`や個人用の`notes/`だけに残さない。
新タスクや引き継ぎは`presswatch-task-handoff-ja`に従う。

## Git操作

- `status`、`diff`、`log`、`show`、ブランチ一覧、`fetch`などの確認は実行できる。
- worktree・index・ローカル履歴を変えるGit操作、ブランチ作成・削除、push、PR作成・マージは、対象と影響を示して明示的な了承後に行う。
- ブランチ作成前にGit状態、親ブランチ、作業ブランチ案を報告する。
  Phase運用とPR baseは`phase-branch-workflow-ja`に従う。
- commit・push・PR作成は、検証結果と全差分の再監査結果を提示した後に改めて許可を得る。

## 検証と完了

- 製品の観測可能な振る舞いを追加・変更する場合は`presswatch-tdd-ja`に従う。
- 文書・スキルなど製品の振る舞いを変えない変更は、構造・参照先・差分を検証し、形式的なテストを追加しない。
- `git diff --check`と全差分のセルフレビューを行い、変更内容、検証結果、未実施の確認と理由を報告する。
