# リポジトリ指示

## 言語

- 明示的に別言語を指定されない限り、ユーザーへの応答、計画、レビュー、作成する文書は日本語で記述する。
- 既存文書が主に英語で管理されている場合は、文書内の言語を維持してよい。

## プロジェクト概要

- PressWatchは、環境省の報道発表を収集、整理、検索しやすくする個人開発プロジェクトである。
- 現場の情報収集負担と見逃しリスクを減らすことを目的とする。
- MVPの価値、可読性、保守性、安全性を優先し、必要性が確認できない複雑さを加えない。

## 常に守る境界

- `.env`、`.env.*`、認証情報ファイル、秘密鍵ファイルは、ユーザーの明示的な許可がない限り読まない。
- APIキー、パスワード、トークン、接続文字列などの秘密情報を、ログ、提案コード、文書、コミット候補へ含めない。
- 変更前に、今回扱うことと扱わないことを明確にする。
- 必要のないファイルやユーザーの既存変更へ手を加えず、差分を小さく保つ。
- 要件にない機能、大規模リファクタリング、依存関係を追加しない。
- ユーザーの指示、`task.md`、本ファイル、選択したスキル、関連docsの間に矛盾がある場合は、編集を止めて確認する。

## 情報の正本

- 現在の一タスクに固有の目的、範囲、除外事項、確認条件: `task.md`
- 製品要件とMVP範囲: `docs/requirements.md`
- プロジェクト全体のタスクと完了状態: `docs/tasks.md`
- 技術選定とバージョン方針: `docs/tech-stack.md`と各パッケージのマニフェスト
- ローカル開発手順と実行コマンド: `docs/local-development.md`と各パッケージのスクリプト定義
- 継続して共有する設計、運用、開発手順: `docs/`
- 個人の学習、質疑、判断の経緯: `notes/`

`task.md`と`notes/`だけに、継続して共有すべき要件、進捗、開発手順を残さない。
バージョン、コマンド、実装済み範囲は変わりうるため、作業時に正本を確認する。

## リポジトリ構成

- `apps/web`: Next.jsアプリケーション
- `apps/api`: FastAPIアプリケーション
- `packages/scraper`: スクレイピング処理
- `docs`: 共有するプロジェクト文書
- `infra`: Docker関連の設定と補助ファイル

## 実装と検証

- 既存コードの命名、責務分割、ディレクトリ構成に合わせる。
- TypeScriptとPythonでは、既存方針に沿った型情報を維持する。
- 外部入力や取得データには、欠損、形式揺れ、不正値、外部サービスの失敗がありうる前提で扱う。
- エラーを握りつぶさず、原因と利用側から見える失敗時の挙動を明確にする。
- 製品の観測可能な振る舞いを追加または変更する場合は、`presswatch-tdd-ja`に従う。
- 文書、スキル、設定など製品の振る舞いを変えない変更では、形式的なテストを追加せず、対象に合う構造検証、静的確認、差分確認を行う。
- 実在するテスト、lint、typecheck、ビルド、検証コマンドを確認し、存在しないコマンドを推測で実行しない。

## Git操作の承認境界

- `status`、`diff`、`log`、`show`、ブランチ一覧、`fetch`など、worktree、index、ローカル履歴を変えない確認は実行できる。
- `switch`、`pull`、`rebase`、`cherry-pick`、`stash`、`restore`、`reset`など、worktree、index、ローカル履歴を変える操作は、対象と影響を報告し、ユーザーの明示的な了承後に行う。
- ブランチ作成、`git add`、commit、push、PR作成、PRマージ、ブランチ削除も、ユーザーの明示的な了承後に行う。
- Phase開始時は、現在のGit状態、親ブランチ、作業ブランチ案を報告し、了承を得てから作業ブランチを作成する。
- commit、push、PR作成は、検証結果と全差分の再監査結果を提示した後に改めて許可を得る。
- ユーザーの変更や未コミット差分を破棄しない。

## スキルの入口

| 作業場面 | 使用するスキル |
| --- | --- |
| 次のPhaseタスク、新しいチャット、`task.md`による引き継ぎ | `presswatch-task-handoff-ja` |
| Phaseブランチ、タスクブランチ、PR baseの確認 | `phase-branch-workflow-ja` |
| 製品の振る舞いの追加、変更 | `presswatch-tdd-ja` |
| コミット分割、コミットメッセージ | `commit-message-ja` |
| `docs/tasks.md`の完了更新 | `task-update-ja` |
| PressWatch内のMarkdownの作成、更新、レビュー | `presswatch-markdown-style-ja` |
| 個別の学習メモ、設計判断、質疑の記録 | `presswatch-notes-ja` |
| API、ライブラリ、コマンドの使用メモ | `presswatch-usage-notes-ja` |
| 用語集の追加、更新 | `presswatch-glossary-ja` |
| `notes/`全体の索引、重複、読む順番の整理 | `presswatch-notes-index-ja` |
| 行コメント、短いブロックコメント | `comment-style-ja` |
| Python Docstring | `python-docstring-ja` |
| `.codex/skills/`の作成、更新、検証 | `skill-creator`と`presswatch-skill-maintenance-ja` |

複数スキルを使う場合は、それぞれの責務を分け、同じ手順を複数箇所で再定義しない。

## 変更完了時

- 対象に合う検証を実行し、実行できなかった確認と理由を明示する。
- ユーザーから見た振る舞い、既存要件、関連docs、秘密情報の境界に差分がないか確認する。
- `git diff --check`と全差分のセルフレビューを行う。
- 変更したファイル、変更しなかった範囲、検証結果、未解決事項を報告する。
