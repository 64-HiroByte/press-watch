export default function Home() {
  return (
    <main className="page">
      <section className="intro" aria-labelledby="page-title">
        <p className="eyebrow">PressWatch Web</p>
        <h1 id="page-title">環境省の報道発表を確認する土台</h1>
        <p>
          一覧表示、検索、固定カテゴリによる絞り込みは今後のフェーズで実装します。
          ブックマーク機能はMVP後に扱います。
        </p>
      </section>
    </main>
  );
}
